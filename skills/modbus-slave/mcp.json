{
  "mcpServers": {
    "modbus-slave": {
      "command": "modbus-mcp-server",
      "args": ["--transport", "stdio", "--log-level", "INFO"],
      "env": {
        "MODBUS_MCP_DEFAULT_TIMEOUT": "5.0"
      }
    }
  }
}